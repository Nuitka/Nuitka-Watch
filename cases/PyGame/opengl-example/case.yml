# yamllint disable rule:line-length
# yamllint disable rule:indentation
# yamllint disable rule:comments-indentation
# too many spelling things, spell-checker: disable
---
- case: PyGame-latest
  # Integer version, increased when results become invalidated by cases changes.
  version: 1
  filename: "PyGameOpenGLTest.py"
  # Requirements to do
  requirements:
    # First one is item under test.
    - PyOpenGL
    - PyGame
  os:
    - Win32
    - Linux
    - macOS
  nuitka: "1.8"
  python_version_req: "python_version >= 0x380"
  # Does commercial compilation change things.
  commercial: "both"
  # Does this package create a GUI program.
  gui: "yes"
  # Does this package have icons
  icons: "no"
  # Can this program be run automatically.
  interactive: "yes"
- case: pygame_ce-latest
  # Integer version, increased when results become invalidated by cases changes.
  version: 1
  filename: "PyGameOpenGLTest.py"
  # Requirements to do
  requirements:
    # First one is item under test.
    - PyOpenGL
    - pygame_ce
  os:
    - Win32
    - Linux
    - macOS
  nuitka: "2.5"
  python_version_req: "python_version >= 0x380"
  # Does commercial compilation change things.
  commercial: "both"
  # Does this package create a GUI program.
  gui: "yes"
  # Does this package have icons
  icons: "no"
  # Can this program be run automatically.
  interactive: "yes"